import Foundation

public struct Session {
    public static func connect(portal_url: String, token: String) ->  
        let session = Session(portal_url: portal_url, username: username, password: password)
        return session.connect()
    } 
}
